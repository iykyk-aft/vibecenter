node_modules/
# all local state: token, custom apps, gateway state, event log, settings
# backups, and the desktop app-window browser profile
data/
*.cc-backup
*.log
vibecenter/bridge-run.sh

# per-machine Claude Code settings (absolute paths, permissions) — never sync
.claude/settings.local.json
